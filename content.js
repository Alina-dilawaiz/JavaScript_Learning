// const { version } = require("react")

// npm init: when we want to bootstrap the package json file, we use npm init and based on options we provide values. 
// npm install: when we have pakg json file, we use npm install to download dependencies
// * npm install <pkg-name>
// * -g installs pkg globally
// * --save-dev 
// * npm install <pkg-name>@<version>
// * npm update: updates dependencies
// * npm update pkg-name
// * npm run <task-name>
// * npm list 
// * npm view pkg-name
// * npm uninstall
// * npm help 


