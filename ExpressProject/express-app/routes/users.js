var express = require('express');
var router = express.Router();

/* GET users listing. */
res.render('user', {
  title: 'Users page tutorial', 
  message:"Users listed" });

module.exports = router;
